export class Album_Model {
    constructor(public id: number, public title: string, public titleEn: string, public cover: string,
        public orderNumber: number, public shareLink: string) {
    }
}